#include <iostream>
using namespace std;
/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main() {
	
	
	int temp =0;
	
	int a = {3,4,5,6,7} ;
	for(int i=0;i<5;i++){
		for(int j=0;j<5-i;j++){
			if(a[i] < a[j]){
				
				min == j ;
			}
		}
		int temp == a[i];
				 a[i] == a[j+1];
				 a[j + 1] == temp;
	}
		for(int i=0;i<5;i++){
		for(int j=0;j<5;j++){
			cout<<a[i][j];
		}
	}
	
	
	
	
	return 0;
}
